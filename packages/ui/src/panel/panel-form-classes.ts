/** Clases globales de formulario del panel (theme-base.css). */
export const PANEL_INPUT_CLASS = 'form-input min-w-0 w-full';

/** Select nativo con chevron y tokens globales (.form-select). */
export const PANEL_SELECT_CLASS = 'form-select min-w-0 w-full';
