import '../../../../../packages/ui/src/operator-site/operator-site.css';

/** Estilos del perfil público / landing del profesional (solo rutas /[slug]). */
export default function OperatorSiteLayout({ children }: { children: React.ReactNode }) {
    return children;
}
