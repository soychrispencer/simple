# ─────────────────────────────────────────────────────────────────────────────
# simpleagenda — variables de entorno
# Copia este archivo a .env.local y completa los valores reales
# Puerto de desarrollo: 3004
# ─────────────────────────────────────────────────────────────────────────────

# ── API ───────────────────────────────────────────────────────────────────────
# Desarrollo: ambas apuntan al API local.
# Producción (Coolify): NEXT_PUBLIC_API_URL vacío + API_INTERNAL_URL=https://api.simpleplataforma.app
NEXT_PUBLIC_API_URL=http://localhost:4000
API_INTERNAL_URL=http://localhost:4000

# ── App ───────────────────────────────────────────────────────────────────────
# URL pública de esta app (links de reserva, SEO, schema.org)
NEXT_PUBLIC_APP_URL=http://localhost:3004

# ── Google Maps — Places Autocomplete ────────────────────────────────────────
# Browser key con "Places API" habilitada.
# Google Console → Credenciales → Restricciones de referrer HTTP:
#   localhost:*          (desarrollo)
#   simpleagenda.cl/*    (producción)
NEXT_PUBLIC_GOOGLE_MAPS_BROWSER_KEY=

# ── Google AI (Gemini) ────────────────────────────────────────────────────────
# Genera políticas de cancelación con IA. Si está vacía usa texto predefinido.
# Obtener en: https://aistudio.google.com/app/apikey
GOOGLE_AI_API_KEY=

# ── Mercado Pago (checkout Pro vía API) ───────────────────────────────────────
# La integración vive en services/api/.env.local:
#   MERCADO_PAGO_ACCESS_TOKEN=APP_USR-...
#   MERCADO_PAGO_PUBLIC_ORIGIN_AGENDA=http://localhost:3004
#   MP_AGENDA_APP_ID=... / MP_AGENDA_APP_SECRET=... (OAuth cobros)
NEXT_PUBLIC_MERCADO_PAGO_PUBLIC_KEY=APP_USR-cc536e87-a22e-4f8d-8f41-a340c0a9c51e
