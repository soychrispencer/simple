# ─────────────────────────────────────────────────────────────────────────────
# SimplePropiedades — Variables de entorno
# Copia este archivo a .env.local para desarrollo local.
# En producción (Coolify) configura cada variable en el panel de tu servicio.
#
# IMPORTANTE: Las variables NEXT_PUBLIC_* se hornean en el build.
# Si las cambias en Coolify debes hacer redeploy completo (no solo restart).
# ─────────────────────────────────────────────────────────────────────────────

# ── API Backend ────────────────────────────────────────────────────────────────
# Requerido. URL pública del contenedor API.
# Desarrollo: http://localhost:4000
# Producción: https://api.simpleplataforma.app
NEXT_PUBLIC_API_URL=http://localhost:4000

# ── URL pública de esta app ────────────────────────────────────────────────────
# Usada en sitemap.xml, robots.txt y Open Graph.
# Desarrollo: http://localhost:3001
NEXT_PUBLIC_APP_URL=https://simplepropiedades.cl

# ── Google Maps / Places (autocompletado de direcciones) ──────────────────────
# Opcional. Sin esta key el campo de dirección funciona sin sugerencias.
# Restringir en Google Cloud Console a tu dominio de producción.
NEXT_PUBLIC_GOOGLE_MAPS_BROWSER_KEY=AIzaSy-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
