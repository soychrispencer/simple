/** IDs de secciones en la landing (scroll por hash en `/`). */
export const LANDING_HASH_SECTION_IDS = new Set([
    'hero',
    'para-clientes',
    'destacados',
    'como-funciona',
    'musicos',
]);
