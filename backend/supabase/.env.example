# =================================================================
# Supabase CLI & Backend Scripts Configuration
# =================================================================
# Used by Supabase CLI (e.g., migrations) and other backend scripts.
# Copy this file to .env in this directory for local development.

# --- Supabase Project ---
SUPABASE_PROJECT_ID=your-project-id
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key

# --- Supabase Admin ---
# Required for admin-level operations (e.g., seeding data, migrations).
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# --- Supabase Direct DB Connection ---
# Used by scripts that connect directly to the database.
SUPABASE_STAGING_DB_URL=postgresql://postgres:password@aws-1-<region>.pooler.supabase.com:6543/postgres?sslmode=require&prefer_simple_protocol=true
SUPABASE_PROD_DB_URL=postgresql://postgres:password@aws-1-<region>.pooler.supabase.com:6543/postgres?sslmode=require&prefer_simple_protocol=true

# =================================================================
# Services (for Backend Scripts)
# =================================================================

# --- hCaptcha ---
# Secret key for verifying hCaptcha tokens in backend services.
HCAPTCHA_SECRET_KEY=

# --- Email / SMTP ---
# For scripts that need to send emails.
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
LEADS_NOTIFY_TO=

# --- Rate Limiting Examples ---
# These might be used by backend services, included here for completeness.
VENTA_ASISTIDA_MAX_PER_HOUR=3
VENTA_ASISTIDA_MAX_PER_DAY=10
