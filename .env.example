# Variables globales (NO commitear secretos).
#
# Recomendación:
# - En cada app Next.js (apps/*) crea `.env.local` copiando desde su `.env.example`.
# - Para scripts/CLI de Supabase, usa `backend/supabase/.env` (copiando desde `backend/supabase/.env.example`).

# Supabase Project Configuration
SUPABASE_PROJECT_ID=your-project-id

# URLs/keys (duplicamos nombres por compatibilidad: apps usan NEXT_PUBLIC_*, scripts usan SUPABASE_*)
SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co

SUPABASE_ANON_KEY=your-anon-key
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Solo server (Server Actions / scripts). NO exponer en el cliente.
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Conexiones directas (usadas por scripts como supabase-push.mjs)
SUPABASE_STAGING_DB_URL=postgresql://postgres:password@aws-1-<region>.pooler.supabase.com:6543/postgres?sslmode=require&prefer_simple_protocol=true
SUPABASE_PROD_DB_URL=postgresql://postgres:password@aws-1-<region>.pooler.supabase.com:6543/postgres?sslmode=require&prefer_simple_protocol=true
